<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--region TextView-->
    <style name="SelectableItem">
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <style name="TitleText" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/fragment_list_title_color</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="SubTitleText" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">@color/fragment_list_sub_title_color</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="DetailTitleText" parent="SubTitleText"/>

    <style name="DetailSubTitleText" parent="TitleText">
        <item name="android:textStyle">normal</item>
    </style>
    <!--endregion-->

</resources>